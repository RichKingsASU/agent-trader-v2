# Frontend runs in local mode by default (no external SaaS required).
# Only set Firebase values if you explicitly want to enable Firebase-backed auth/data.
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_STORAGE_BUCKET=
VITE_FIREBASE_MESSAGING_SENDER_ID=
VITE_FIREBASE_APP_ID=

# Optional: Cloud Functions base URL (used by backtesting page).
VITE_FUNCTIONS_URL=http://localhost:5001

# Optional: Used only to build a Firebase Console link in the header.
VITE_SYNC_FUNCTION_NAME=
VITE_SYNC_FUNCTION_REGION=us-central1
