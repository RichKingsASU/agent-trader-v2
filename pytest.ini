[pytest]
testpaths =
    tests
    backend/time/tests
python_files = test_*.py

# Enable running bare `async def test_*` without explicit markers.
asyncio_mode = auto

markers =
    asyncio: mark a test as using asyncio

