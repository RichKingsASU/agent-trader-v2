apiVersion: v1
kind: Service
metadata:
  name: agenttrader-ops-ui
  namespace: trading-floor
  labels:
    app: agenttrader-ops-ui
spec:
  type: ClusterIP
  selector:
    app: agenttrader-ops-ui
  ports:
    - name: http
      port: 80
      targetPort: http

