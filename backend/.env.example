#
# Canonical local dev env example (DO NOT COMMIT REAL VALUES).
#

# Repo-wide safety hard lock (required by backend/common/agent_mode_guard.py)
AGENT_MODE=OFF
TRADING_MODE=paper

# Market open safety (NYSE equities)
# Default behavior: block equity order placement for the first 15 minutes after market open.
# Override:
# - set EXEC_MARKET_OPEN_TRADE_BLOCK_MINUTES=0 to disable, or
# - set EXEC_MARKET_OPEN_TRADE_BLOCK_DISABLED=1 to force-disable
EXEC_MARKET_OPEN_TRADE_BLOCK_MINUTES=15
EXEC_MARKET_OPEN_TRADE_BLOCK_DISABLED=0

# Alpaca (canonical official SDK env vars)
APCA_API_KEY_ID=
APCA_API_SECRET_KEY=
APCA_API_BASE_URL=https://paper-api.alpaca.markets

# Firebase / Firestore
# Prefer ADC via: `gcloud auth application-default login`
# If you must use a local keyfile, set GOOGLE_APPLICATION_CREDENTIALS (for local/CI only).
FIREBASE_PROJECT_ID=
GOOGLE_APPLICATION_CREDENTIALS=
