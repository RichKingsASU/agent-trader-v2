#
# Canonical local dev env example (DO NOT COMMIT REAL VALUES).
#

# Repo-wide safety hard lock (required by backend/common/agent_mode_guard.py)
AGENT_MODE=OFF
TRADING_MODE=paper

# Paper execution guardrails
# Per-symbol cooldown to prevent overtrading (default 600s = 10 minutes)
TRADE_SYMBOL_COOLDOWN_S=600

# Alpaca (canonical official SDK env vars)
APCA_API_KEY_ID=
APCA_API_SECRET_KEY=
APCA_API_BASE_URL=https://paper-api.alpaca.markets

# Firebase / Firestore
# Prefer ADC via: `gcloud auth application-default login`
# If you must use a local keyfile, set GOOGLE_APPLICATION_CREDENTIALS (for local/CI only).
FIREBASE_PROJECT_ID=
GOOGLE_APPLICATION_CREDENTIALS=
