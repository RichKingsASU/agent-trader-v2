"""
Strategy sandbox runner (Firecracker).

This package is intentionally self-contained and uses only stdlib so the host
backend can package and launch microVMs without importing user strategy code.
"""

