"""vNEXT Portfolio Risk — interface contracts (placeholders only).

This file intentionally contains *no runtime behavior*.
Implementations will be defined later, in separate packages/modules.
"""


# Contract placeholders (intentionally empty)

class Contracts:
    """Namespace for future interface definitions."""

    pass
