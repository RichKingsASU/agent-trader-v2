"""
Institutional observability primitives:
- Agent identity contract (env-sourced, fail-fast)
- Correlation IDs (context propagation)
- Redaction filters (no secrets in logs)
- JSON logger + intent logging schema
"""

