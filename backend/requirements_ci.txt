# CI dependency subset (repo-level test runner)
#
# This intentionally excludes `google-genai` because it requires `websockets>=13`,
# while `alpaca-trade-api` (used by `functions/`) pins `websockets<11`. Installing
# both stacks together is unsatisfiable and prevents CI from running tests.
#
# Production services should continue using `backend/requirements.txt`.

fastapi==0.126.0
uvicorn==0.38.0

# Market data + transport
alpaca-py==0.43.2
websockets
httpx
requests
tenacity
google-cloud-pubsub

# Firebase / Firestore
firebase-admin
google-cloud-firestore

# Runtime config / messaging / utilities
python-dotenv
nats-py
pandas
pyyaml

# Contract validation (contracts/ JSON Schemas)
jsonschema==4.26.0

# Optional DB drivers (Postgres)
psycopg[binary]
asyncpg==0.29.0
psycopg2-binary==2.9.9

# Repo blueprint generator (k8s/cloudbuild YAML parsing)
PyYAML==6.0.1

