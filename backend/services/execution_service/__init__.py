"""
Execution Engine service (Cloud Run).

This is a thin HTTP wrapper around `backend.execution.engine.ExecutionEngine`.
It is intentionally small so it can be deployed independently from other APIs.
"""

