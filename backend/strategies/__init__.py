"""
Repo-native strategy modules + configuration registry.

This package is intentionally safe-by-default: strategies must be explicitly
enabled via versioned config files under `configs/strategies/`.
"""

