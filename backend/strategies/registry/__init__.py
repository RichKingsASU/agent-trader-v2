"""
Strategy Config Registry (Git-native, safe-by-default).

Loads and validates versioned strategy configs from `configs/strategies/` and
exposes a read-only interface for services like strategy-engine.
"""

