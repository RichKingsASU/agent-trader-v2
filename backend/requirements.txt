fastapi==0.126.0
uvicorn==0.38.0

# Market data + transport
alpaca-py==0.43.2
websockets
httpx
requests
tenacity
google-genai
google-cloud-pubsub

# Firebase / Firestore
firebase-admin
google-cloud-firestore
google-genai

# Runtime config / messaging / utilities
python-dotenv
nats-py
pandas
pyyaml

# Contract validation (contracts/ JSON Schemas)
jsonschema

# Optional DB drivers (Postgres)
psycopg[binary]
asyncpg==0.29.0
psycopg2-binary==2.9.9

# Repo blueprint generator (k8s/cloudbuild YAML parsing)
# NOTE: Do not force a PyYAML minimum here. The repo-level CI installs both
# backend and Cloud Functions dependencies, and `alpaca-trade-api` currently
# pins PyYAML to 6.0.1. Keeping this unconstrained avoids CI resolver failures.
