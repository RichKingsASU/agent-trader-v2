fastapi==0.126.0
uvicorn==0.38.0

# Market data + transport
alpaca-py==0.43.2
websockets
httpx
requests
tenacity
google-genai
google-cloud-pubsub
google-cloud-secret-manager

# Firebase / Firestore
firebase-admin
google-cloud-firestore
google-genai

# Runtime config / messaging / utilities
nats-py
pandas
pyyaml

# Contract validation (contracts/ JSON Schemas)
jsonschema

# Optional DB drivers (Postgres)
psycopg[binary]
asyncpg==0.29.0
psycopg2-binary==2.9.9

# Repo blueprint generator (k8s/cloudbuild YAML parsing)
PyYAML>=6.0.2
