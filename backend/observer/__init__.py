"""
Observer modules (read-mostly).

This package hosts tooling intended to *observe and explain* system behavior.
It must not place orders or call brokers. The only permitted writes are
explicit "explanation records" for audit/debugging.
"""

