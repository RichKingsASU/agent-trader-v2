"""
Trading domain package.

Note: This package intentionally contains *non-executing* artifacts such as
order proposals (auditable trade intents) which are safe to emit from strategy
workloads without broker integration.
"""

