"""
Order Proposal contract (signals -> auditable, non-executing trade intents).

This package defines a shared proposal schema, validation rules, and an emitter
that writes append-only NDJSON audit artifacts.
"""

