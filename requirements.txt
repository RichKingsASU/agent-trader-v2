# Repository-level dependency aggregator used by CI.
#
# Keep this file lightweight and explicit; it should include the deps needed to
# run tests/compile checks. Cloud Functions dependencies are installed separately
# (see requirements.functions.txt) because they intentionally have a different
# dependency graph than the backend runtime.
-r backend/requirements.txt
-r base_requirements.txt