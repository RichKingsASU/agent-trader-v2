# Repository-level dependency aggregator used by CI.
#
# Keep this file lightweight and explicit; it should include the deps needed to
# run tests/compile checks and the Firebase Python functions.
-r backend/requirements.txt
-r functions/requirements.txt

# Test runner (repo uses pytest in /tests)
pytest>=8.3.0

