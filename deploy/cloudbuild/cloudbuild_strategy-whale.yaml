steps:
  - name: "gcr.io/google.com/cloudsdktool/cloud-sdk"
    args:
      - "build"
      - "-f"
      - "apps/strategy-whale/Dockerfile"
      - "-t"
      - "us-east4-docker.pkg.dev/${PROJECT_ID}/trader-repo/strategy-whale:${SHORT_SHA}"
      - "-t"
      - "us-east4-docker.pkg.dev/${PROJECT_ID}/trader-repo/strategy-whale:latest"
images:
  - "us-east4-docker.pkg.dev/${PROJECT_ID}/trader-repo/strategy-whale:${SHORT_SHA}"
  - "us-east4-docker.pkg.dev/${PROJECT_ID}/trader-repo/strategy-whale:latest"
