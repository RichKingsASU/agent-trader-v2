steps:
  - name: "gcr.io/google.com/cloudsdktool/cloud-sdk"
    args:
      - "build"
      - "-f"
      - "apps/marketdata/Dockerfile"
      - "-t"
      - "us-east4-docker.pkg.dev/${PROJECT_ID}/trader-repo/marketdata:${SHORT_SHA}"
      - "-t"
      - "us-east4-docker.pkg.dev/${PROJECT_ID}/trader-repo/marketdata:latest"
images:
  - "us-east4-docker.pkg.dev/${PROJECT_ID}/trader-repo/marketdata:${SHORT_SHA}"
  - "us-east4-docker.pkg.dev/${PROJECT_ID}/trader-repo/marketdata:latest"
