alpaca-py==0.43.2
alpaca-trade-api==3.2.0
firebase-admin==6.5.0
firebase-functions==0.5.0
pydantic>=2.0.0,<3.0.0
cloudevents==1.11.0
# NOTE: google-cloud-aiplatform>=1.92 pulls in google-genai (websockets>=13),
# which conflicts with alpaca-trade-api's websocket dependency constraints.
# Pin to the newest version that does not require google-genai so CI installs remain resolvable.
google-cloud-aiplatform==1.91.0
google-cloud-secret-manager>=2.26.0
pytz==2024.1
