steps:
- name: gcr.io/cloud-builders/bash
  entrypoint: bash
  args:
    [
      "build",
      "-f",
      "backend/strategy_service/Dockerfile",
      "-t",
      "us-east4-docker.pkg.dev/agenttrader-prod/trader-repo/strategy-runtime:${SHORT_SHA}",
      ".",
    ]
images:
- "us-east4-docker.pkg.dev/agenttrader-prod/trader-repo/strategy-runtime:${SHORT_SHA}"
