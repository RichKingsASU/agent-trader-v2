fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.9.2
python-multipart==0.0.12
authlib==1.3.2
itsdangerous==2.2.0
httpx==0.27.2
aiofiles==23.2.1

# Firebase for reading intent history
firebase-admin==6.5.0
google-cloud-firestore==2.19.0

# Reuse existing backend dependencies
# (These should already be in the environment when deployed alongside backend)
# If running standalone, uncomment:
# alpaca-py==0.43.2
