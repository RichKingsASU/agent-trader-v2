# Copy to: /etc/agenttrader/congressional-ingest.env
# Then: sudo chown root:agenttrader /etc/agenttrader/congressional-ingest.env && sudo chmod 0640 /etc/agenttrader/congressional-ingest.env
#
# IMPORTANT:
# - Do not echo these values in shell scripts.
# - systemd reads this file directly; it will not print values.
TENANT_ID=prod
AGENT_MODE=OFF

# Messaging
NATS_URL=nats://nats.prod:4222

# Polling
POLL_INTERVAL_SECONDS=3600
LOOKBACK_DAYS=7

# Quiver (required for non-mock prod ingest)
QUIVER_API_KEY=__REDACTED__

