# Copy to: /etc/agenttrader/market-ingest.env
# Then: sudo chown root:agenttrader /etc/agenttrader/market-ingest.env && sudo chmod 0640 /etc/agenttrader/market-ingest.env
#
# IMPORTANT:
# - Do not echo these values in shell scripts.
# - systemd reads this file directly; it will not print values.
#
# Required tenancy
TENANT_ID=prod
AGENT_MODE=OFF

# Alpaca (required for live ingest)
APCA_API_KEY_ID=__REDACTED__
APCA_API_SECRET_KEY=__REDACTED__
APCA_API_BASE_URL=https://paper-api.alpaca.markets
ALPACA_DATA_FEED=iex
MONITORED_SYMBOLS=SPY

# Firestore auth/project (pick ONE approach)
# If using a service account JSON on disk:
#GOOGLE_APPLICATION_CREDENTIALS=/etc/agenttrader/gcp-sa.json
FIRESTORE_PROJECT_ID=__REDACTED__

# Optional tuning (safe defaults exist in code)
#DRY_RUN=0
#HEARTBEAT_INTERVAL_S=15
#PER_SYMBOL_MIN_INTERVAL_MS=1000
#GLOBAL_WRITES_PER_SEC=20
#GLOBAL_BURST=40
#FLUSH_INTERVAL_MS=200

